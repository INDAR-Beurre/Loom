<UserControl x:Class="LoomBrowser.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,40,0,0">
            <Border Width="120" Height="120" CornerRadius="28" Background="#174b34" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid>
                    <Ellipse Fill="#2eb66d" Opacity="0.95" Margin="10" />
                    <TextBlock Text="" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
            </Border>
            <TextBlock Text="Loom" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,14,0,0" Foreground="#a7efc0"/>
            <TextBlock Text="A privacy-first AI browser interface" FontSize="13" Opacity="0.85" HorizontalAlignment="Center" Margin="0,4,0,0" Foreground="#8fcaa2"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="40" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search bar -->
            <Border Grid.Row="0" Margin="0,18,0,0" Background="#0b3e2a" CornerRadius="32" Padding="6" BorderBrush="#134d33" BorderThickness="1" HorizontalAlignment="Center" Width="640" Effect="{DynamicResource DropShadowEffect}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🔍" VerticalAlignment="Center" Margin="12,0" Foreground="#9fe3a7"/>
                    <TextBox x:Name="SearchTextBox" Grid.Column="1" Margin="8,0" VerticalAlignment="Center" FontSize="16" BorderThickness="0" Background="Transparent" Width="440" Height="36" Foreground="#e6ffe9" />
                    <Button x:Name="SearchButton" Grid.Column="2" Content="Search" Width="92" Height="32" Margin="8,0" HorizontalAlignment="Right" Background="#1c7a46" Foreground="White" BorderThickness="0"/>
                </Grid>
            </Border>

            <!-- Quick Links -->
            <ItemsControl Grid.Row="1" Margin="0,18,0,0" HorizontalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1" Columns="4" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <Button Tag="https://www.youtube.com" Click="QuickLink_Click" Margin="8" Width="150" Height="72" Background="#0e3e2b" BorderBrush="#1f5b3f" BorderThickness="1">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border Width="44" Height="44" CornerRadius="10" Background="#143b2a" HorizontalAlignment="Center"><TextBlock Text="📺" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        <TextBlock Text="YouTube" HorizontalAlignment="Center" Margin="4,6,4,0" Foreground="#cfeede" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Tag="https://github.com" Click="QuickLink_Click" Margin="8" Width="150" Height="72" Background="#0e3e2b" BorderBrush="#1f5b3f" BorderThickness="1">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border Width="44" Height="44" CornerRadius="10" Background="#143b2a" HorizontalAlignment="Center"><TextBlock Text="🐙" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        <TextBlock Text="GitHub" HorizontalAlignment="Center" Margin="4,6,4,0" Foreground="#cfeede" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Tag="https://reddit.com" Click="QuickLink_Click" Margin="8" Width="150" Height="72" Background="#0e3e2b" BorderBrush="#1f5b3f" BorderThickness="1">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border Width="44" Height="44" CornerRadius="10" Background="#143b2a" HorizontalAlignment="Center"><TextBlock Text="🤖" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        <TextBlock Text="Reddit" HorizontalAlignment="Center" Margin="4,6,4,0" Foreground="#cfeede" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Tag="https://wikipedia.org" Click="QuickLink_Click" Margin="8" Width="150" Height="72" Background="#0e3e2b" BorderBrush="#1f5b3f" BorderThickness="1">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border Width="44" Height="44" CornerRadius="10" Background="#143b2a" HorizontalAlignment="Center"><TextBlock Text="📚" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        <TextBlock Text="Wikipedia" HorizontalAlignment="Center" Margin="4,6,4,0" Foreground="#cfeede" FontSize="11"/>
                    </StackPanel>
                </Button>
            </ItemsControl>

            <!-- Credits / tagline -->
            <StackPanel Grid.Row="2" VerticalAlignment="Top" Margin="0,36,0,0" HorizontalAlignment="Center">
                <TextBlock Text="Search suggestions, AI assistant, and simulated extensions are available." Opacity="0.8" TextAlignment="Center" Width="720"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
