<UserControl x:Class="LoomBrowser.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="800">
    <Grid Background="Transparent">
        <StackPanel Margin="24">
            <TextBlock x:Name="QueryHeading" Text="Search" FontSize="22" FontWeight="Bold" Margin="0,6"/>
            <TextBlock x:Name="MetaText" Text="Results from DuckDuckGo" Opacity="0.8" FontSize="12"/>

            <Border Margin="0,12,0,0" Background="White" CornerRadius="8" Padding="18">
                <StackPanel>
                    <TextBlock x:Name="AbstractText" Text="Loading..." TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
